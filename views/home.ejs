<%- include('partials/header.ejs') %>
  <section class="mt-3">
    <div class="container">
      <div class="row ">
        <% newListItem.forEach(function(item) { %>
          <div class="col-md-3 p-3 ">
            <div class="card" style="height:28rem;width: 16rem;">
              <img src=" <%= item.bookURL %>" class="card-img " alt="...">
              <div class="card-body">
                <h5 class="card-title">
                  <%= item.booktitle %>
                </h5>
                <p>
                  <%= item.bookauthor %>
                </p>
                <button class="btn btn-primary">View</button>
              </div>
            </div>
          </div>
          <% }) %>
      </div>
    </div>
  </section>